'use client';

import React from 'react';
import { motion, AnimatePresence, useScroll, useTransform, useMotionValue, useSpring, Reorder } from 'framer-motion';

export default function CandlestickDemo() {
  const [hoveredCandle, setHoveredCandle] = React.useState(null);
  const [mousePos, setMousePos] = React.useState({ x: 0, y: 0 });
  const [timeframe, setTimeframe] = React.useState('1D');
  const [showVolume, setShowVolume] = React.useState(true);
  const [showGrid, setShowGrid] = React.useState(true);
  const [showIndicators, setShowIndicators] = React.useState(false);

  const dates = ['Jan 8', 'Jan 9', 'Jan 10', 'Jan 11', 'Jan 12', 'Jan 15', 'Jan 16', 'Jan 17', 'Jan 18', 'Jan 19', 'Jan 22', 'Jan 23', 'Jan 24', 'Jan 25', 'Jan 26'];
  const candles = [
    { open: 170, close: 175, high: 178, low: 168, volume: 52.3 },
    { open: 175, close: 172, high: 177, low: 170, volume: 48.1 },
    { open: 172, close: 180, high: 182, low: 171, volume: 67.8 },
    { open: 180, close: 177, high: 183, low: 175, volume: 41.2 },
    { open: 177, close: 185, high: 188, low: 176, volume: 73.5 },
    { open: 185, close: 182, high: 187, low: 180, volume: 38.9 },
    { open: 182, close: 190, high: 192, low: 181, volume: 61.4 },
    { open: 190, close: 188, high: 193, low: 186, volume: 44.7 },
    { open: 188, close: 195, high: 198, low: 187, volume: 78.2 },
    { open: 195, close: 192, high: 197, low: 190, volume: 35.6 },
    { open: 192, close: 200, high: 203, low: 191, volume: 82.1 },
    { open: 200, close: 198, high: 202, low: 196, volume: 29.3 },
    { open: 198, close: 205, high: 208, low: 197, volume: 91.7 },
    { open: 205, close: 202, high: 207, low: 200, volume: 42.8 },
    { open: 202, close: 210, high: 212, low: 201, volume: 88.4 },
  ];
  const minPrice = 165; const maxPrice = 215; const maxVolume = 100;
  const scale = (v) => ((v - minPrice) / (maxPrice - minPrice)) * 200;
  const volumeScale = (v) => (v / maxVolume) * 50;
  const currentCandle = hoveredCandle !== null ? candles[hoveredCandle] : candles[candles.length - 1];
  const currentDate = hoveredCandle !== null ? dates[hoveredCandle] : dates[dates.length - 1];
  const priceChange = currentCandle.close - currentCandle.open;
  const priceChangePercent = ((priceChange / currentCandle.open) * 100).toFixed(2);
  const ma5 = candles.map((_, i) => i < 4 ? null : candles.slice(i - 4, i + 1).reduce((acc, c) => acc + c.close, 0) / 5);
  const gridLevels = [minPrice, (minPrice + maxPrice) / 2, maxPrice];

  return (<><div className="demo-header"><h2 className="demo-title">Candlestick Chart</h2><p className="demo-subtitle">Professional chart with advanced interactive controls</p></div><div className="demo-area"><div className="candlestick-chart" style={{ height: 580 }}><div className="candlestick-header" style={{ marginBottom: 16 }}><div><div className="candlestick-title">AAPL</div><div style={{ color: '#666', fontSize: '0.8rem' }}>Apple Inc. • {currentDate}</div></div><div style={{ textAlign: 'right' }}><motion.div className="candlestick-price" key={currentCandle.close} initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }}>${currentCandle.close.toFixed(2)}</motion.div><motion.div key={priceChange} initial={{ scale: 1.1 }} animate={{ scale: 1 }} style={{ color: priceChange >= 0 ? '#22c55e' : '#ef4444', fontSize: '0.85rem' }}>{priceChange >= 0 ? '+' : ''}{priceChange.toFixed(2)} ({priceChangePercent}%)</motion.div></div></div><div style={{ display: 'flex', gap: 12, marginBottom: 16, flexWrap: 'wrap', alignItems: 'center' }}><div style={{ display: 'flex', gap: 4, background: 'rgba(255,255,255,0.03)', padding: 4, borderRadius: 8 }}>{['1H', '1D', '1W', '1M', '1Y'].map(tf => (<motion.button key={tf} onClick={() => setTimeframe(tf)} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }} style={{ padding: '6px 12px', background: timeframe === tf ? 'rgba(99, 102, 241, 0.3)' : 'transparent', border: timeframe === tf ? '1px solid rgba(99, 102, 241, 0.5)' : '1px solid transparent', borderRadius: 6, color: timeframe === tf ? '#a5b4fc' : '#666', fontSize: '0.75rem', cursor: 'pointer', fontWeight: timeframe === tf ? 600 : 400, transition: 'all 0.2s' }}>{tf}</motion.button>))}</div><div style={{ display: 'flex', gap: 8, marginLeft: 'auto' }}><motion.button onClick={() => setShowVolume(!showVolume)} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }} style={{ padding: '6px 12px', background: showVolume ? 'rgba(34, 197, 94, 0.2)' : 'rgba(255,255,255,0.05)', border: '1px solid rgba(255,255,255,0.1)', borderRadius: 6, color: showVolume ? '#22c55e' : '#666', fontSize: '0.75rem', cursor: 'pointer' }}>Vol</motion.button><motion.button onClick={() => setShowGrid(!showGrid)} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }} style={{ padding: '6px 12px', background: showGrid ? 'rgba(34, 197, 94, 0.2)' : 'rgba(255,255,255,0.05)', border: '1px solid rgba(255,255,255,0.1)', borderRadius: 6, color: showGrid ? '#22c55e' : '#666', fontSize: '0.75rem', cursor: 'pointer' }}>Grid</motion.button><motion.button onClick={() => setShowIndicators(!showIndicators)} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }} style={{ padding: '6px 12px', background: showIndicators ? 'rgba(34, 197, 94, 0.2)' : 'rgba(255,255,255,0.05)', border: '1px solid rgba(255,255,255,0.1)', borderRadius: 6, color: showIndicators ? '#22c55e' : '#666', fontSize: '0.75rem', cursor: 'pointer' }}>MA</motion.button></div></div><AnimatePresence>{hoveredCandle !== null && (<motion.div initial={{ opacity: 0, height: 0, y: -10 }} animate={{ opacity: 1, height: 'auto', y: 0 }} exit={{ opacity: 0, height: 0, y: -10 }} transition={{ duration: 0.25, ease: 'easeOut' }} style={{ display: 'flex', gap: 24, padding: '14px 16px', background: 'linear-gradient(135deg, rgba(99, 102, 241, 0.08), rgba(168, 85, 247, 0.08))', borderLeft: `3px solid ${priceChange >= 0 ? '#22c55e' : '#ef4444'}`, borderRadius: 8, fontSize: '0.85rem', marginBottom: 12, backdropFilter: 'blur(10px)' }}><div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}><span style={{ color: '#666', fontSize: '0.7rem' }}>OPEN</span><span style={{ fontWeight: 600 }}>${currentCandle.open.toFixed(2)}</span></div><div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}><span style={{ color: '#666', fontSize: '0.7rem' }}>HIGH</span><span style={{ color: '#22c55e', fontWeight: 600 }}>${currentCandle.high.toFixed(2)}</span></div><div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}><span style={{ color: '#666', fontSize: '0.7rem' }}>LOW</span><span style={{ color: '#ef4444', fontWeight: 600 }}>${currentCandle.low.toFixed(2)}</span></div><div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}><span style={{ color: '#666', fontSize: '0.7rem' }}>CLOSE</span><span style={{ fontWeight: 600 }}>${currentCandle.close.toFixed(2)}</span></div><div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}><span style={{ color: '#666', fontSize: '0.7rem' }}>VOLUME</span><span style={{ color: '#a78bfa', fontWeight: 600 }}>{currentCandle.volume.toFixed(1)}M</span></div><div style={{ display: 'flex', flexDirection: 'column', gap: 2, marginLeft: 'auto' }}><span style={{ color: '#666', fontSize: '0.7rem' }}>CHANGE</span><span style={{ color: priceChange >= 0 ? '#22c55e' : '#ef4444', fontWeight: 600 }}>{priceChange >= 0 ? '+' : ''}{priceChange.toFixed(2)} ({priceChangePercent}%)</span></div></motion.div>)}</AnimatePresence><div style={{ flex: 1, display: 'flex', flexDirection: 'column' }}><div className="candlestick-area" style={{ flex: 1, paddingBottom: 10, position: 'relative' }}><AnimatePresence>{showGrid && gridLevels.map((price, i) => (<motion.div key={price} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} style={{ position: 'absolute', left: 0, right: 0, bottom: scale(price), height: 1, background: 'rgba(255,255,255,0.05)', pointerEvents: 'none' }}><span style={{ position: 'absolute', right: -45, top: -8, fontSize: '0.7rem', color: '#555' }}>${price.toFixed(0)}</span></motion.div>))}</AnimatePresence><AnimatePresence>{hoveredCandle !== null && (<><motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} style={{ position: 'absolute', left: hoveredCandle * 42 + 20, top: 0, bottom: 0, width: 2, background: 'linear-gradient(180deg, rgba(99, 102, 241, 0.4), rgba(168, 85, 247, 0.4))', pointerEvents: 'none', boxShadow: '0 0 10px rgba(99, 102, 241, 0.5)' }} /><motion.div initial={{ opacity: 0, scale: 0.8, y: 10 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: 0.8, y: 10 }} transition={{ duration: 0.2 }} style={{ position: 'absolute', left: Math.min(hoveredCandle * 42 + 20, 550), top: -60, background: 'rgba(17, 24, 39, 0.95)', border: `1px solid ${priceChange >= 0 ? 'rgba(34, 197, 94, 0.3)' : 'rgba(239, 68, 68, 0.3)'}`, borderRadius: 10, padding: '10px 14px', fontSize: '0.75rem', pointerEvents: 'none', backdropFilter: 'blur(10px)', boxShadow: '0 10px 30px rgba(0,0,0,0.5)', zIndex: 10 }}><div style={{ fontWeight: 600, marginBottom: 4, color: '#fff' }}>{currentDate}</div><div style={{ color: priceChange >= 0 ? '#22c55e' : '#ef4444', fontWeight: 600 }}>${currentCandle.close.toFixed(2)} ({priceChange >= 0 ? '+' : ''}{priceChangePercent}%)</div></motion.div></>)}</AnimatePresence><AnimatePresence>{showIndicators && (<motion.svg initial={{ opacity: 0 }} animate={{ opacity: 0.6 }} exit={{ opacity: 0 }} style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', pointerEvents: 'none' }}><motion.path d={ma5.map((ma, i) => ma !== null ? `${i === 0 || ma5[i-1] === null ? 'M' : 'L'} ${i * 42 + 20} ${200 - scale(ma)}` : '').join(' ')} stroke="#f59e0b" strokeWidth={2} fill="none" initial={{ pathLength: 0 }} animate={{ pathLength: 1 }} transition={{ duration: 1.5, ease: 'easeOut' }} /></motion.svg>)}</AnimatePresence>{candles.map((candle, i) => { const isGreen = candle.close >= candle.open; const bodyTop = Math.max(candle.open, candle.close); const bodyBottom = Math.min(candle.open, candle.close); const bodyHeight = Math.max(scale(bodyTop) - scale(bodyBottom), 4); const wickTopHeight = scale(candle.high) - scale(bodyTop); const wickBottomHeight = scale(bodyBottom) - scale(candle.low); const isHovered = hoveredCandle === i; return (<motion.div key={i} className="candle" style={{ color: isGreen ? '#22c55e' : '#ef4444', width: 36 }} initial={{ opacity: 0, y: 50 }} animate={{ opacity: isHovered || hoveredCandle === null ? 1 : 0.4, y: 0 }} transition={{ delay: i * 0.04, type: 'spring', stiffness: 200 }} onHoverStart={() => setHoveredCandle(i)} onHoverEnd={() => setHoveredCandle(null)} whileHover={{ scale: 1.15, zIndex: 5 }}><div className="candle-wick" style={{ height: wickTopHeight }} /><motion.div className="candle-body" style={{ height: bodyHeight, background: isGreen ? '#22c55e' : '#ef4444', width: isHovered ? 22 : 16 }} animate={isHovered ? { boxShadow: `0 0 30px ${isGreen ? '#22c55e' : '#ef4444'}`, filter: 'brightness(1.3)' } : { boxShadow: 'none', filter: 'brightness(1)' }} transition={{ duration: 0.2 }} /><div className="candle-wick" style={{ height: wickBottomHeight }} /></motion.div>); })}</div><AnimatePresence>{showVolume && (<motion.div initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: 70 }} exit={{ opacity: 0, height: 0 }} transition={{ duration: 0.3 }} style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'center', gap: 8, borderTop: '1px solid rgba(255,255,255,0.08)', paddingTop: 10 }}>{candles.map((candle, i) => { const isGreen = candle.close >= candle.open; const isHovered = hoveredCandle === i; return (<motion.div key={i} initial={{ height: 0 }} animate={{ height: volumeScale(candle.volume), opacity: isHovered || hoveredCandle === null ? 1 : 0.3 }} transition={{ delay: i * 0.04 + 0.3, type: 'spring' }} style={{ width: isHovered ? 22 : 16, background: isGreen ? 'rgba(34, 197, 94, 0.5)' : 'rgba(239, 68, 68, 0.5)', borderRadius: 3, position: 'relative' }} onHoverStart={() => setHoveredCandle(i)} onHoverEnd={() => setHoveredCandle(null)}>{isHovered && (<motion.div initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} style={{ position: 'absolute', bottom: '100%', left: '50%', transform: 'translateX(-50%)', marginBottom: 4, fontSize: '0.7rem', color: '#a78bfa', fontWeight: 600, whiteSpace: 'nowrap' }}>{candle.volume.toFixed(1)}M</motion.div>)}</motion.div>); })}</motion.div>)}</AnimatePresence><div style={{ display: 'flex', justifyContent: 'center', gap: 8, marginTop: 10 }}>{dates.map((date, i) => (<motion.span key={i} animate={{ color: hoveredCandle === i ? '#fff' : '#444', scale: hoveredCandle === i ? 1.1 : 1 }} transition={{ duration: 0.2 }} style={{ width: 36, textAlign: 'center', fontSize: '0.65rem', fontWeight: hoveredCandle === i ? 600 : 400 }}>{i % 3 === 0 ? date.split(' ')[1] : ''}</motion.span>))}</div></div><AnimatePresence>{showIndicators && (<motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} style={{ marginTop: 12, display: 'flex', gap: 16, fontSize: '0.75rem', color: '#666' }}><div style={{ display: 'flex', alignItems: 'center', gap: 6 }}><div style={{ width: 20, height: 2, background: '#f59e0b' }} /><span>MA(5)</span></div></motion.div>)}</AnimatePresence></div></div></>);
}
